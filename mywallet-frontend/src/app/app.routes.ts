import { Routes } from '@angular/router';
import { ExpenseFormComponent } from './forms/expense-form.component';

export const routes: Routes = [
    { path: 'form-expense', component: ExpenseFormComponent }
];
